{
  "name": "are-the-brewers-on-tv",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "update-data": "node update-data.js",
    "render:capture": "node scripts/render-check.mjs capture",
    "render:diff": "node scripts/render-check.mjs diff",
    "test": "node --test",
    "build-indices": "node --max-old-space-size=3000 scripts/build-indices.mjs",
    "check-indices": "node --max-old-space-size=3000 scripts/build-indices.mjs --check"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "opentype.js": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
